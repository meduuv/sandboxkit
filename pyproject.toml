[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "sandboxkit"
version = "0.1.0"
description = "Sandbox policy metadata helpers"
requires-python = ">=3.9"
[tool.setuptools]
py-modules = ["sandboxkit"]
